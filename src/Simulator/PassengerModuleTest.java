package simulator;

/*
import static org.junit.Assert.assertTrue;

import java.util.ArrayList;

import org.junit.Test;

import model.Bus;
import model.BusStop;

public class PassengerModuleTest
{

    @Test
    public void transferPassengerstest()
    {
        ArrayList<BusStop> schedule = new ArrayList<BusStop>();
        schedule.add( new BusStop( "Petla", 3 ) );
        schedule.add( new BusStop( "Pierwszy", 5 ) );
        schedule.add( new BusStop( "Drugi", 3 ) );
        
        Bus bus = new Bus( schedule );
        PassengerModule passengerModule = new PassengerModule();
        
        passengerModule.setPassenger( schedule.get( 0 ), schedule.get( 2 ) );
        
        passengerModule.transferPassengers( bus );
        
        // Checks if Bus takes in passengers from the BusStop
        assertTrue( !bus.getPassengerMap().isEmpty() );
        //TODO 
    }

}
*/